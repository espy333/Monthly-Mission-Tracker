# Monthly-Mission-Tracker
This application uses Java and the Spring Boot to track monthly goals(missions).
REST APIs to manage and interact goals through standard CRUD operations.
JPA and H2 database to store the goals.
